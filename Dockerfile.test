FROM ubuntu:22.04

# Install system dependencies
RUN apt-get update && \
    apt-get install -y \
    bats \
    git \
    bash \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /workspace

# Default command
CMD ["/bin/bash"]

